export type LoginPage = boolean;
export type ThemeButton = boolean | null | string | number;
export type themeSchema = {
  title: string;
  id: number;
  theme: string;
  icon: string;
};
export type themeIndex = number;
